@model AIS_WEB.Models.PlantViewModel

@{
    ViewBag.Title = "PassValue";
}

<h2>PassValue</h2>

<ul>
    @for (int i = 0; i < ViewBag.Numb; i++)
    {
        <li>
            @ViewBag.Name;
        </li>
    }

</ul>